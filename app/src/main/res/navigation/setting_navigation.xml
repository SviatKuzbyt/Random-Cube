<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/setting_navigation"
    app:startDestination="@id/numbers_menu">

    <fragment
        android:id="@+id/numbers_menu"
        android:name="ua.sviatkuzbyt.randomcube.ui.numbers.NumbersFragment"
        android:label="fragment_numbers"
        tools:layout="@layout/fragment_numbers" />
    <fragment
        android:id="@+id/words_menu"
        android:name="ua.sviatkuzbyt.randomcube.ui.words.WordsFragment"
        android:label="fragment_words"
        tools:layout="@layout/fragment_words" />
    <fragment
        android:id="@+id/categories_menu"
        android:name="ua.sviatkuzbyt.randomcube.ui.categories.CategoriesFragment"
        android:label="fragment_categories"
        tools:layout="@layout/fragment_categories" />
</navigation>